let my_name: string = "Farhan Ahmed"; // This saves the name

console.log(my_name.toLowerCase()); // Shows the name in all small letters
console.log(my_name.toUpperCase()); // Shows the name in ALL BIG LETTERS
console.log(my_name.charAt(0).toUpperCase() + my_name.slice(1).toLowerCase()); // Shows the name with the First Letter Big